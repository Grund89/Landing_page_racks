<div class="container">
  <h1>Blog da Dra. Raquel</h1>
  <p>Confira dicas e guias sobre seus direitos trabalhistas e previdenciários.</p>

  <% @articles.each do |article| %>
    <div class="section">
      <h2><%= link_to article.title, article_path(article) %></h2>
      <p><%= article.content.truncate(150) %>...</p>
    </div>
  <% end %>
</div>